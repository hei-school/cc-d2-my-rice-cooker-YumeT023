/**
 * @param {number} foodCup
 * @return {number}
 */
export const getEstimatedCookDurationByFoodCupSeconds = (foodCup = 0) => {
  // we are modern:D (for simplicity purpose)
  return foodCup * 5;
};
